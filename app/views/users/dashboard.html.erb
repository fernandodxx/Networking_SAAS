<div class="container mx-auto p-4">
  <h1 class="text-2xl font-semibold">Bem-vindo, <%= current_user.email %>!</h1>
  <div class="mt-4">
    <h2 class="text-lg font-semibold">Suas Conexões</h2>
    <ul>
      <% current_user.connected_users.each do |user| %>
        <li class="p-2 border-b"><%= user.email %></li>
      <% end %>
    </ul>
  </div>
</div>
